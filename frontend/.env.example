## Next.js (client + server)
# Backend API base URL (should include /api suffix)
NEXT_PUBLIC_API_URL=http://localhost:5001/api

# Public site URL (used for canonical URLs)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

